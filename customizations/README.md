# Baserow Customizations

Any file or folder structured inside this directory will overwrite Baserow's core files during the Docker image build.

### Directory Structure Examples:
*   To modify the backend, mirror the path:
    `customizations/backend/src/baserow/...`
*   To modify the frontend, mirror the path:
    `customizations/web-frontend/plugins/...`
